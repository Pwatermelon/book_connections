# Book Connections - Анализ онтологий из текстов

Профессиональная система для автоматического извлечения именованных сущностей и построения онтологий из текстовых документов. Поддерживает анализ русскоязычных и англоязычных текстов с визуализацией связей в виде интерактивного графа.

## Возможности

### 🔍 Извлечение именованных сущностей
- **Персоны**: Автоматическое распознавание имен людей из текста
- **Локации**: Извлечение географических названий (города, страны, регионы)
- **Организации**: Определение названий организаций и учреждений
- Использует комбинацию методов:
  - Natasha (русскоязычный NER)
  - pymorphy2 для морфологического анализа и нормализации
  - transformers с русскими моделями (опционально)

### 🔗 Анализ связей между сущностями
Система извлекает связи только на основе явных упоминаний в тексте:
- **RESIDENCE** (Проживание): связь между персоной и местом (например, "Иван из Саратова")
- **FAMILY** (Родство): семейные связи (брат, сестра, родители и т.д.)
- **FRIENDSHIP** (Дружба): дружеские отношения
- **WORK** (Работа): рабочие и профессиональные отношения
- **LOVE** (Любовь): романтические отношения

### 📊 Построение онтологии
- Структурированное представление знаний в виде онтологии
- Экспорт в формате OWL для работы в Protege
- Метаданные о сущностях (количество упоминаний, типы связей)

### 🎨 Визуализация
- Интерактивный граф связей с подписями на ребрах
- Цветовая кодировка типов сущностей и связей
- Адаптивная раскладка узлов
- Легенда и детальная информация при наведении

### 🌐 Веб-интерфейс
- Современный минималистичный интерфейс
- Drag-and-drop загрузка файлов
- Автоматическое определение кодировки текста
- Просмотр результатов онлайн

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Скачайте модели для Natasha (происходит автоматически при первом использовании)

3. Для веб-интерфейса запустите:
```bash
python app.py
```

Затем откройте браузер и перейдите на http://127.0.0.1:5000

## Использование

### Базовое использование

```bash
python main.py путь_к_книге.txt
```

### С указанием выходного файла для графа

```bash
python main.py книга.txt граф.html
```

### Пример

```bash
python main.py war_and_peace.txt graph.html
```

## Формат входных данных

Программа принимает текстовый файл (`.txt`) в кодировке UTF-8 с текстом книги.

## Выходные файлы

После обработки создаются:

1. **graph.html** (или указанный файл) - интерактивный граф связей
   - Откройте в браузере для просмотра
   - Наведите курсор на узлы и рёбра для детальной информации
   - Размер узлов зависит от количества связей

2. **ontology.owl** - онтология в формате OWL
   - Можно открыть в Protege для просмотра и редактирования
   - Содержит все сущности, классы и связи в стандартном формате OWL/RDF
   - Включает метаданные (количество упоминаний, типы связей)

3. **ontology.txt** (при использовании CLI) - текстовое представление онтологии
   - Список всех сущностей с их типами
   - Список всех связей с контекстом

## Структура проекта

```
book_connections/
├── app.py                  # Flask веб-приложение
├── main.py                 # CLI интерфейс
├── entity_extractor.py     # Извлечение именованных сущностей
├── relation_extractor.py   # Извлечение связей между сущностями
├── ontology_builder.py     # Построение онтологии и экспорт в OWL
├── graph_visualizer.py     # Визуализация графа (NetworkX + Plotly)
├── templates/
│   └── index.html          # Веб-интерфейс
├── uploads/                # Загруженные файлы
├── results/                # Результаты анализа (графы, OWL файлы)
├── requirements.txt        # Зависимости Python
└── README.md              # Документация
```

## Технологии

- **NLP**: Natasha, pymorphy2, transformers
- **Онтологии**: owlready2 (OWL/RDF)
- **Визуализация**: NetworkX, Plotly
- **Веб**: Flask
- **Язык**: Python 3.8+

## Типы связей

Связи создаются **двусторонними**:
- 🔴 **RESIDENCE** (Проживание) - связь персоны с местом
  - Обратная: **HAS_RESIDENT** (Живут здесь) - связь места с персоной
- 🟣 **FAMILY** (Родство) - семейные связи (симметричные)
- 🔵 **FRIENDSHIP** (Дружба) - дружеские отношения (симметричные)
- 🟠 **WORK** (Работа) - рабочие отношения
- 💖 **LOVE** (Любовь) - романтические отношения

Например, если "Иван из Саратова", создаются две связи:
- Иван → Саратов (RESIDENCE)
- Саратов → Иван (HAS_RESIDENT)

## Примеры использования

### Тест на примере
В проекте есть пример файла `example_book.txt` для тестирования:
```bash
python main.py example_book.txt example_graph.html
```

### Анализ классического романа
```bash
python main.py "Война и мир.txt" war_and_peace_graph.html
```

### Анализ современного романа
```bash
python main.py novel.txt output.html
```

## Технические детали

### Извлечение сущностей
1. **Нативный NER через Natasha**: Основной метод для русского языка
2. **Морфологический анализ**: Проверка через pymorphy2 для валидации имен
3. **Резервный метод**: Transformers с русскими моделями (если установлены)

### Извлечение связей
- Используются паттерны на основе ключевых слов
- Связи извлекаются только при явном упоминании в тексте
- Поддержка вариаций написаний (разные падежи имен)

### Нормализация
- Все локации приводятся к именительному падежу через pymorphy2
- Имена персон нормализуются и группируются (Иван Петров, Ивана Петрова → Иван Петров)

### Формат OWL
Онтология экспортируется в стандартном формате OWL/RDF с:
- Классами: Person, Location, Organization
- Объектными свойствами: hasResidence, hasResident, hasFamily, hasFriend, etc.
- Индивидами: все найденные сущности
- Фактами: все связи между сущностями

## Особенности

- ✅ Двусторонние связи (например, не только "Иван → Саратов", но и "Саратов → Иван")
- ✅ Автоматическое определение кодировки файлов (UTF-8, Windows-1251, CP866 и др.)
- ✅ Обработка больших текстов
- ✅ Интерактивная визуализация с возможностью масштабирования

## Ограничения

- Программа оптимизирована для русского языка (английские имена могут распознаваться хуже)
- Для больших текстов (100+ страниц) обработка может занять несколько минут
- Качество извлечения связей зависит от стиля написания текста
- Связи извлекаются только при явных упоминаниях (не инференция)

## Требования

- Python 3.8+
- Все зависимости указаны в `requirements.txt`
- Для улучшенного NER (опционально): установите transformers и torch

## Лицензия

Проект создан в образовательных целях.
